Sources for the plugin `plugin.appodeal.InMobi`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.InMobi" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
